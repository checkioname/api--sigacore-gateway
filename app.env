# ============================================
# CONFIGURAÇÃO DE AMBIENTE - DESENVOLVIMENTO
# ============================================
# AVISO: Este arquivo contém configurações para desenvolvimento.
# EM PRODUÇÃO, use variáveis de ambiente do sistema operacional!

# Ambiente da aplicação (development, production, testing)
ENVIRONMENT=development

# ============================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ============================================
# Em produção, use credenciais seguras e SSL obrigatório
DB_SOURCE=postgresql://admin:admin@localhost:5432/catalog?sslmode=disable

# ============================================
# CONFIGURAÇÃO DE TOKENS (CRÍTICO!)
# ============================================
# ATENÇÃO: A chave abaixo é APENAS para desenvolvimento!
# EM PRODUÇÃO, gere uma chave criptograficamente segura de 32 caracteres:
# 
# Como gerar uma chave segura:
# 1. Linux/macOS: openssl rand -hex 16
# 2. Online: https://www.random.org/passwords/?num=1&len=32&format=plain
# 3. Go: crypto/rand
#
# NUNCA use a chave padrão em produção!
TOKEN_SYMMETRIC_KEY=DV_KEY_NOT_FOR_PRODUCTION_USE_32

# Duração dos tokens
ACCESS_TOKEN_DURATION=15m
REFRESH_TOKEN_DURATION=24h

# ============================================
# ENDEREÇOS DOS SERVIÇOS
# ============================================
GATEWAY_SERVER_ADDRESS=localhost:8080
AUTH_SERVER_ADDRESS=localhost:8081

# Microserviços (adapte conforme sua arquitetura)
USER_SERVICE_ADDRESS=http://localhost:8082
DOC_SERVICE_ADDRESS=http://localhost:8083
NOTIFICATION_SERVICE_ADDRESS=http://localhost:8084

# ============================================
# CONFIGURAÇÃO DO GATEWAY
# ============================================
# Lista de IPs permitidos (separados por vírgula)
# Em desenvolvimento: use localhost
# Em produção: use IPs específicos ou redes CIDR
ALLOWED_IPS=127.0.0.1,::1, localhost

# ============================================
# INSTRUÇÕES PARA PRODUÇÃO
# ============================================
# 1. NÃO use este arquivo em produção
# 2. Configure as variáveis de ambiente no sistema:
#    export ENVIRONMENT=production
#    export TOKEN_SYMMETRIC_KEY="sua-chave-segura-de-32-caracteres"
#    export DB_SOURCE="postgresql://user:senha@host:5432/db?sslmode=require"
# 3. Use um serviço de gestão de segredos (AWS Secrets Manager, etc.)
# 4. Configure monitoramento e alertas
# 5. Use HTTPS e certificados válidos